import { SituacaoCartorioModel } from './situacao-cartorio-model.model';

describe('SituacaoCartorioModel', () => {
  it('should create an instance', () => {
    expect(new SituacaoCartorioModel()).toBeTruthy();
  });
});
